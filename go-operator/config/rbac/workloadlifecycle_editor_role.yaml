# This rule is not used by the project go-operator itself.
# It is provided to allow the cluster admin to help manage permissions for users.
#
# Grants permissions to create, update, and delete resources within the lifecycle.ai-infra.demo.
# This role is intended for users who need to manage these resources
# but should not control RBAC or manage permissions for others.

apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  labels:
    app.kubernetes.io/name: go-operator
    app.kubernetes.io/managed-by: kustomize
  name: workloadlifecycle-editor-role
rules:
- apiGroups:
  - lifecycle.ai-infra.demo
  resources:
  - workloadlifecycles
  verbs:
  - create
  - delete
  - get
  - list
  - patch
  - update
  - watch
- apiGroups:
  - lifecycle.ai-infra.demo
  resources:
  - workloadlifecycles/status
  verbs:
  - get
