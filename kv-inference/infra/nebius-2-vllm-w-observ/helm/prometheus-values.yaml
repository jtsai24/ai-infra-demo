server:
  service:
    type: NodePort
    nodePort: 30090

serverFiles:
  prometheus.yml:
    scrape_configs:
      - job_name: vllm
        static_configs:
          - targets:
            - vllm.default.svc.cluster.local:8000